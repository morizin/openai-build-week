import { PenLine } from "lucide-react";
import { LmsShell } from "@/components/lms-shell";
const threads=["Dr. Elena Ruiz|Feedback on your distribution analysis|Your explanation is heading in the right direction. Review the note on...|12 min", "Marcus Bell|Workshop materials for Thursday|I uploaded the examples we will use during the live workshop.|2 hr", "Nadia Ahmed|Project group confirmed|Your project group is now available in the course workspace.|Yesterday"];
export default function InboxPage(){return <LmsShell active="Inbox" eyebrow="COMMUNICATION" title="Inbox" actions={<button className="lms-primary"><PenLine size={16}/> New message</button>}><p className="fixture-note">Preview data — messaging APIs are not connected yet.</p><section className="inbox-layout"><div className="thread-list"><div className="filter-row"><button className="selected">All</button><button>Unread · 3</button></div>{threads.map((t,i)=>{const [name,subject,copy,time]=t.split("|");return <article className={i===0?"selected":""} key={subject}><span>{name.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><strong>{name}</strong><b>{subject}</b><p>{copy}</p></div><small>{time}</small></article>})}</div><div className="message-view"><span>DATA-101 · DATA LITERACY FOUNDATIONS</span><h2>Feedback on your distribution analysis</h2><div className="message-meta"><b>ER</b><p><strong>Dr. Elena Ruiz</strong><small>To Maya Chen · 12 minutes ago</small></p></div><p>Hi Maya,</p><p>Your explanation is heading in the right direction. Review the note on how a single extreme value changes the mean, then compare that with what happens to the median.</p><p>You can revise the assignment until 5:00 PM today.</p><p>— Elena</p><button className="lms-secondary">Reply</button></div></section></LmsShell>}
