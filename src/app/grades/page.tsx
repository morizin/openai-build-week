import { Download } from "lucide-react";
import { LmsShell } from "@/components/lms-shell";
import { courses } from "@/lib/lms-data";
export default function GradesPage(){return <LmsShell active="Grades" eyebrow="ACADEMIC RECORD" title="Grades" actions={<button className="lms-secondary"><Download size={16}/> Export</button>}><p className="fixture-note">Preview data — grade APIs are not connected yet.</p><div className="grade-summary"><div><span>CURRENT TERM</span><strong>89.4%</strong><small>Overall weighted average</small></div><div><span>CREDITS</span><strong>12</strong><small>In progress</small></div><div><span>SUBMISSIONS</span><strong>25 / 27</strong><small>92.6% on time</small></div></div><section className="data-table grade-table"><div className="table-row table-head"><span>COURSE</span><span>ASSIGNMENTS</span><span>CURRENT GRADE</span><span>LETTER</span><span>TREND</span></div>{courses.map((c,i)=><div className="table-row" key={c.code}><strong>{c.title}<small>{c.code}</small></strong><span>{5+i} / {6+i}</span><span>{[92.5,87.2,94.1,83.8][i]}%</span><span>{["A-","B+","A","B"][i]}</span><span className="positive">↑ {[1.2,.8,2.4,.3][i]}%</span></div>)}</section></LmsShell>}
